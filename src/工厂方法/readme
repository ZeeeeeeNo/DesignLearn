简单工厂的优缺点
	优点：工厂类中包含了必要的判断逻辑，根据客户端的选中条件动态实例化相关的类，对于客户端来说
	去除了具体产品的依赖
	缺点：当你需要添加新的产品的时候，需要去修改工厂的方法，修改原来类
	这点违反了开放-封闭原则
	
工厂方式模式就是来解决简单工厂的缺点的
它将工厂抽象出来，变成了一个工厂抽象接口和多个具体生成对象的工厂
但是我们需要添加新产品的时候，只需要新增此产品对应的产品类和工厂类就可以了，无需去修改工厂的方法

但是这里有点比较困惑，工厂方法把简单工厂原来在工厂内部的逻辑转移到了客户端中进行，你需要加功能的话，就由修改工厂类转为修改客户端
缺点：每加一个产品，就需要加一个产品的工厂类，增添加额外的开发量